#include <bits/stdc++.h>

using namespace std;

#define endl '\n';

int main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    int a, b, h;

    cin >> a;
    cin >> b;
    cin >> h;

    cout << (a + b) * h / 2 << endl;

    return 0;
}